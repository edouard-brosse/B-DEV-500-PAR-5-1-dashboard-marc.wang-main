import React, { useState, useEffect, useContext } from 'react'

const Logout = () => {

    const [checked, setChecked] = useState(false);

    return (
        <div className="logoutContainer">
            <button>decconnection</button>
        </div>
    )
}

export default Logout